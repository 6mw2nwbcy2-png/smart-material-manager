name: Disabled stone contact hotfix

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled - do not modify budget or transaction data"
